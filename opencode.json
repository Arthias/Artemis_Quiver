{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["npx", "-y", "@playwright/mcp", "--allowed-hosts", "localhost", "--port", "3099"],
      "enabled": true,
      "timeout": 120000
    }
  }
}
